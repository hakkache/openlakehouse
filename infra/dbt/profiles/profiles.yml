openlakehouse:
  target: dev
  outputs:
    dev:
      type: trino
      method: none
      user: dbt
      host: trino
      port: 8080
      catalog: iceberg
      schema: dbt_marts
      http_scheme: http
      threads: 2
